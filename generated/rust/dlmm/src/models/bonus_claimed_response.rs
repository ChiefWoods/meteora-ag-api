/*
 * DLMM API
 *
 * Indexed DLMM data for pools, portfolios, positions, limit orders, wallets, and protocol stats.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// BonusClaimedResponse : Realized bonus a wallet has claimed on one pool. Aggregates every cancel event the wallet has on the pool, including cancels on orders that have not yet been closed. USD/SOL values are priced from each cancel row's own `amount_*_usd` / `amount_*_sol` ratios (cancel-time prices), not current spot.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct BonusClaimedResponse {
    /// Number of distinct cancel events contributing to these totals.
    #[serde(rename = "cancel_event_count")]
    pub cancel_event_count: i64,
    /// `total_bonus_x_sol + total_bonus_y_sol`.
    #[serde(rename = "total_bonus_sol")]
    pub total_bonus_sol: String,
    /// `total_bonus_x_usd + total_bonus_y_usd`.
    #[serde(rename = "total_bonus_usd")]
    pub total_bonus_usd: String,
    /// Bonus token X claimed, in token X display units (decimals applied).
    #[serde(rename = "total_bonus_x")]
    pub total_bonus_x: String,
    /// Bonus token X claimed, in SOL (cancel-time SOL price).
    #[serde(rename = "total_bonus_x_sol")]
    pub total_bonus_x_sol: String,
    /// Bonus token X claimed, in USD (cancel-time price).
    #[serde(rename = "total_bonus_x_usd")]
    pub total_bonus_x_usd: String,
    /// Bonus token Y claimed, in token Y display units (decimals applied).
    #[serde(rename = "total_bonus_y")]
    pub total_bonus_y: String,
    /// Bonus token Y claimed, in SOL (cancel-time SOL price).
    #[serde(rename = "total_bonus_y_sol")]
    pub total_bonus_y_sol: String,
    /// Bonus token Y claimed, in USD (cancel-time price).
    #[serde(rename = "total_bonus_y_usd")]
    pub total_bonus_y_usd: String,
}

impl BonusClaimedResponse {
    /// Realized bonus a wallet has claimed on one pool. Aggregates every cancel event the wallet has on the pool, including cancels on orders that have not yet been closed. USD/SOL values are priced from each cancel row's own `amount_*_usd` / `amount_*_sol` ratios (cancel-time prices), not current spot.
    pub fn new(cancel_event_count: i64, total_bonus_sol: String, total_bonus_usd: String, total_bonus_x: String, total_bonus_x_sol: String, total_bonus_x_usd: String, total_bonus_y: String, total_bonus_y_sol: String, total_bonus_y_usd: String) -> BonusClaimedResponse {
        BonusClaimedResponse {
            cancel_event_count,
            total_bonus_sol,
            total_bonus_usd,
            total_bonus_x,
            total_bonus_x_sol,
            total_bonus_x_usd,
            total_bonus_y,
            total_bonus_y_sol,
            total_bonus_y_usd,
        }
    }
}

