/*
 * DLMM API
 *
 * Indexed DLMM data for pools, portfolios, positions, limit orders, wallets, and protocol stats.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// LimitOrderSummaryResponse : Summary totals for a user's limit orders across every pool. `_sol` variants follow two different time bases: - `total_deposit_sol` — SOL value at *placement time* (SOL price frozen when   the PlaceLimitOrderEvt was indexed). - `total_bonus_sol` — SOL value at *current spot* (current SOL price applied   to the running bonus accrual).  \"Open\" means placed with no row in `close_limit_orders`. \"Closed\" means an `close_limit_orders` row exists. Cancel events are not terminal — a cancelled-but-not-closed order is still open.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct LimitOrderSummaryResponse {
    /// Number of limit orders that have been closed on-chain.
    #[serde(rename = "closed_orders")]
    pub closed_orders: i64,
    /// Number of limit orders that are still open (no close event).
    #[serde(rename = "open_orders")]
    pub open_orders: i64,
    /// Total SOL value of bonus accrued across open orders (current spot SOL price).
    #[serde(rename = "total_bonus_sol")]
    pub total_bonus_sol: String,
    /// Total USD value of bonus (pro-rata fee share) accrued across open orders.
    #[serde(rename = "total_bonus_usd")]
    pub total_bonus_usd: String,
    /// Total SOL value of currently-open order deposits (placement-time SOL price).
    #[serde(rename = "total_deposit_sol")]
    pub total_deposit_sol: String,
    /// Total USD value of currently-open order deposits.
    #[serde(rename = "total_deposit_usd")]
    pub total_deposit_usd: String,
}

impl LimitOrderSummaryResponse {
    /// Summary totals for a user's limit orders across every pool. `_sol` variants follow two different time bases: - `total_deposit_sol` — SOL value at *placement time* (SOL price frozen when   the PlaceLimitOrderEvt was indexed). - `total_bonus_sol` — SOL value at *current spot* (current SOL price applied   to the running bonus accrual).  \"Open\" means placed with no row in `close_limit_orders`. \"Closed\" means an `close_limit_orders` row exists. Cancel events are not terminal — a cancelled-but-not-closed order is still open.
    pub fn new(closed_orders: i64, open_orders: i64, total_bonus_sol: String, total_bonus_usd: String, total_deposit_sol: String, total_deposit_usd: String) -> LimitOrderSummaryResponse {
        LimitOrderSummaryResponse {
            closed_orders,
            open_orders,
            total_bonus_sol,
            total_bonus_usd,
            total_deposit_sol,
            total_deposit_usd,
        }
    }
}

