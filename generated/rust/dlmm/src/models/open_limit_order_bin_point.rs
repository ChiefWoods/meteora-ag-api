/*
 * DLMM API
 *
 * Indexed DLMM data for pools, portfolios, positions, limit orders, wallets, and protocol stats.
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// OpenLimitOrderBinPoint : Per-bin point in an open limit order's distribution. Amounts are in the input (deposit) token's display units — token X for an ask, token Y for a bid — matching the order-level `filled_input_amount`.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct OpenLimitOrderBinPoint {
    #[serde(rename = "bin_id")]
    pub bin_id: i32,
    /// Original deposit at this bin in input-token display units. The frontend's bar-chart heights are derived from `deposit_amount × price` (i.e. the notional in output-token terms) — no per-bin USD field is exposed since order-level totals carry the dollar/SOL values.
    #[serde(rename = "deposit_amount")]
    pub deposit_amount: String,
    #[serde(rename = "fill_status")]
    pub fill_status: models::LimitOrderBinFillStatus,
    /// Filled portion in input-token display units.
    #[serde(rename = "fulfilled_amount")]
    pub fulfilled_amount: String,
    /// Output-token amount received from this bin's fills, in output-token display units. Ask: `fulfilled_amount × price`. Bid: `fulfilled_amount / price`.
    #[serde(rename = "output_received_amount")]
    pub output_received_amount: String,
    /// Price at this bin (token Y per token X).
    #[serde(rename = "price")]
    pub price: String,
    /// Remaining unfilled portion in input-token display units.
    #[serde(rename = "unfilled_amount")]
    pub unfilled_amount: String,
}

impl OpenLimitOrderBinPoint {
    /// Per-bin point in an open limit order's distribution. Amounts are in the input (deposit) token's display units — token X for an ask, token Y for a bid — matching the order-level `filled_input_amount`.
    pub fn new(bin_id: i32, deposit_amount: String, fill_status: models::LimitOrderBinFillStatus, fulfilled_amount: String, output_received_amount: String, price: String, unfilled_amount: String) -> OpenLimitOrderBinPoint {
        OpenLimitOrderBinPoint {
            bin_id,
            deposit_amount,
            fill_status,
            fulfilled_amount,
            output_received_amount,
            price,
            unfilled_amount,
        }
    }
}

