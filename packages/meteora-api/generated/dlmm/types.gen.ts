// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: 'https://dlmm.datapi.meteora.ag' | 'https://dlmm.dev.metdev.io' | (string & {});
};

export type AddressPath = {
    address: string;
};

export type AmountTotals = {
    amount_usd: number;
    amount_x: number;
    amount_y: number;
};

/**
 * Realized bonus a wallet has claimed on one pool. Aggregates every cancel
 * event the wallet has on the pool, including cancels on orders that have
 * not yet been closed. USD/SOL values are priced from each cancel row's own
 * `amount_*_usd` / `amount_*_sol` ratios (cancel-time prices), not current
 * spot.
 */
export type BonusClaimedResponse = {
    /**
     * Number of distinct cancel events contributing to these totals.
     */
    cancel_event_count: number;
    /**
     * `total_bonus_x_sol + total_bonus_y_sol`.
     */
    total_bonus_sol: string;
    /**
     * `total_bonus_x_usd + total_bonus_y_usd`.
     */
    total_bonus_usd: string;
    /**
     * Bonus token X claimed, in token X display units (decimals applied).
     */
    total_bonus_x: string;
    /**
     * Bonus token X claimed, in SOL (cancel-time SOL price).
     */
    total_bonus_x_sol: string;
    /**
     * Bonus token X claimed, in USD (cancel-time price).
     */
    total_bonus_x_usd: string;
    /**
     * Bonus token Y claimed, in token Y display units (decimals applied).
     */
    total_bonus_y: string;
    /**
     * Bonus token Y claimed, in SOL (cancel-time SOL price).
     */
    total_bonus_y_sol: string;
    /**
     * Bonus token Y claimed, in USD (cancel-time price).
     */
    total_bonus_y_usd: string;
};

/**
 * Per-order lifecycle row for a closed limit order in a specific pool.
 * Pool-level metadata (pair, bin step, fees, tokens) lives once at the top of
 * the response — see `ClosedLimitOrdersForPoolResponse`.
 */
export type ClosedLimitOrderDetail = {
    filled_input_amount: string;
    filled_pct: string;
    input_token: string;
    input_token_mint: string;
    is_ask_side: boolean;
    /**
     * Unix seconds. Block time of the close event.
     */
    last_closed_at: number;
    limit_order_address: string;
    lower_pool_price: number;
    /**
     * Unix seconds.
     */
    opened_at: number;
    opened_at_signature: string;
    opened_at_slot: number;
    /**
     * Expected output at placement bin prices — `amount_y` for asks,
     * `amount_x` for bids. Pre-computed on the
     * `limit_orders_aggregate.output_amount_expected` column by the open
     * MV via `if(is_ask_side, amount_y, amount_x)`; the closed-detail SQL
     * reads the stored value directly. Mirrors
     * `OpenLimitOrderDetail::output_amount_expected`.
     */
    output_amount_expected: string;
    output_token: string;
    output_token_mint: string;
    /**
     * Output token amount received for the filled portion of this order, in
     * output-token display units. Sourced from cancel events: the cancel
     * instruction emits the output-side token alongside any unfilled deposit
     * remainder. For an ask order this is `total_withdrawal_y`; for a bid
     * order it is `total_withdrawal_x`.
     */
    received_output_amount: string;
    terminal_signature: string;
    terminal_slot: number;
    /**
     * SOL value of bonus (cancel-event-time SOL price).
     */
    total_bonus_sol: string;
    total_bonus_usd: string;
    total_bonus_x: string;
    /**
     * SOL value of token X bonus (cancel-event-time SOL price).
     */
    total_bonus_x_sol: string;
    /**
     * USD value of token X bonus (cancel-event-time USD price).
     */
    total_bonus_x_usd: string;
    total_bonus_y: string;
    /**
     * SOL value of token Y bonus (cancel-event-time SOL price).
     */
    total_bonus_y_sol: string;
    /**
     * USD value of token Y bonus (cancel-event-time USD price).
     */
    total_bonus_y_usd: string;
    /**
     * SOL value of deposits, deposit-side only (placement-time SOL price).
     */
    total_deposit_sol: string;
    /**
     * USD value of deposits, deposit-side only (an ask deposits X, a bid deposits Y).
     */
    total_deposit_usd: string;
    total_deposit_x: string;
    total_deposit_y: string;
    /**
     * SOL value of withdrawals (cancel-event-time SOL price).
     */
    total_withdrawal_sol: string;
    total_withdrawal_usd: string;
    total_withdrawal_x: string;
    /**
     * SOL value of token X withdrawn (cancel-event-time SOL price).
     */
    total_withdrawal_x_sol: string;
    /**
     * USD value of token X withdrawn (cancel-event-time USD price).
     */
    total_withdrawal_x_usd: string;
    total_withdrawal_y: string;
    /**
     * SOL value of token Y withdrawn (cancel-event-time SOL price).
     */
    total_withdrawal_y_sol: string;
    /**
     * USD value of token Y withdrawn (cancel-event-time USD price).
     */
    total_withdrawal_y_usd: string;
    upper_pool_price: number;
    user_address: string;
};

export type ClosedLimitOrderPoolSummary = {
    filled_pct: string;
    fully_filled_orders: number;
    /**
     * Unix seconds. Latest close-event block time for this pool.
     */
    last_closed_at: number;
    pool: LimitOrderPoolDetails;
    /**
     * SOL value of bonus (cancel-event-time SOL price).
     */
    total_bonus_sol: string;
    total_bonus_usd: string;
    total_bonus_x: string;
    /**
     * SOL value of token X bonus (cancel-event-time SOL price).
     */
    total_bonus_x_sol: string;
    /**
     * USD value of token X bonus (cancel-event-time USD price).
     */
    total_bonus_x_usd: string;
    total_bonus_y: string;
    /**
     * SOL value of token Y bonus (cancel-event-time SOL price).
     */
    total_bonus_y_sol: string;
    /**
     * USD value of token Y bonus (cancel-event-time USD price).
     */
    total_bonus_y_usd: string;
    /**
     * SOL value of deposits, deposit-side only (placement-time SOL price).
     */
    total_deposit_sol: string;
    /**
     * USD value of deposits, deposit-side only (an ask deposits X, a bid deposits Y).
     */
    total_deposit_usd: string;
    total_deposit_x: string;
    /**
     * SOL value of token X deposits (placement-time SOL price).
     */
    total_deposit_x_sol: string;
    /**
     * USD value of token X deposits (placement-time price; only ask orders deposit X).
     */
    total_deposit_x_usd: string;
    total_deposit_y: string;
    /**
     * SOL value of token Y deposits (placement-time SOL price).
     */
    total_deposit_y_sol: string;
    /**
     * USD value of token Y deposits (placement-time price; only bid orders deposit Y).
     */
    total_deposit_y_usd: string;
    total_orders: number;
    /**
     * SOL value of withdrawals (cancel-event-time SOL price).
     */
    total_withdrawal_sol: string;
    total_withdrawal_usd: string;
    total_withdrawal_x: string;
    /**
     * SOL value of token X withdrawn (cancel-event-time SOL price).
     */
    total_withdrawal_x_sol: string;
    /**
     * USD value of token X withdrawn (cancel-event-time USD price).
     */
    total_withdrawal_x_usd: string;
    total_withdrawal_y: string;
    /**
     * SOL value of token Y withdrawn (cancel-event-time SOL price).
     */
    total_withdrawal_y_sol: string;
    /**
     * USD value of token Y withdrawn (cancel-event-time USD price).
     */
    total_withdrawal_y_usd: string;
};

/**
 * Top-level response for `GET /wallets/{wallet}/limit_orders/closed/pools/{pool_address}`.
 * Pool metadata is emitted once at the top; per-order rows in `data` only
 * carry order-specific fields.
 */
export type ClosedLimitOrdersForPoolResponse = {
    current_page: number;
    data: Array<ClosedLimitOrderDetail>;
    page_size: number;
    pages: number;
    pool: LimitOrderPoolDetails;
    total: number;
};

export type ClosedPositionsResponse = {
    closed_at: number;
    created_at: number;
    lower_bin_id: number;
    pnl: number;
    pnl_change_pct: number;
    pool_address: string;
    position_address: string;
    total_claimed_fees: AmountTotals;
    total_deposits: AmountTotals;
    total_withdraws: AmountTotals;
    upper_bin_id: number;
};

export type CumulativeMetrics = {
    fees: number;
    volume: number;
};

export type CurrentPosition = {
    current_deposits: AmountTotals;
    unclaimed_fees: AmountTotals;
    updated_at_slot: number;
};

export type ErrorResponse = {
    message: string;
};

export type GetGroupDetailsParams = {
    lexical_order_mints: string;
};

export type GetGroupsQuery = {
    /**
     * Time window to aggregate fee tvl ratio. Returns Max
     */
    fee_tvl_ratio_tw?: string | null;
    /**
     * Conditions to filter documents by field values
     *
     * Format: `<expr> [&& <expr> ...]`
     *
     * Where each expression is: `<field><op><value>`
     *
     * Allowed fields:
     * - Numeric: `tvl` `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
     * - Boolean: `is_blacklisted`
     * - Text: `pool_address` `name` `token_x` `token_y`
     *
     * Operators:
     * - Numeric: `=` `>` `>=` `<` `<=`
     * - Boolean: `=true` `=false`
     * - Text:
     * - exact match: `=<value>`
     * - multi-value OR: `=[value1|value2|...]`
     *
     * Notes:
     * - Multiple expressions are combined using logical **AND** (`&&`)
     * - Whitespace around operators is ignored
     *
     * Examples:
     * - `tvl>1000`
     * - `is_blacklisted=false && volume_24h>=50000`
     */
    filter_by?: string | null;
    /**
     * Page number (1-based)
     */
    page?: number | null;
    /**
     * Number of pools to return per page. Max 100
     */
    page_size?: number | null;
    /**
     * Search query used to match pools by name, tokens, or address
     */
    query?: string | null;
    /**
     * Sort results by one or more fields
     *
     * Format:
     * - Time-windowed metrics: `<metric>_<window>:<direction>`
     * - Non-windowed metrics: `<field>:<direction>`
     *
     * - `direction`: `asc` or `desc`
     * - `window` (when applicable): `5m` `30m` `1h` `2h` `4h` `12h` `24h`
     *
     * Available fields:
     * - Time-windowed metrics: `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
     * - Non-windowed metrics: `tvl` `fee_pct` `bin_step` `pool_created_at` `farm_apy`
     *
     * Default: `volume_24h:desc`
     *
     * Examples:
     * - `volume_24h:desc`
     * - `fee_1h:asc`
     * - `tvl:desc`
     */
    sort_by?: string | null;
    /**
     * Time window to aggregate volume. Returns sum
     */
    volume_tw?: string | null;
};

/**
 * Query parameters for getting user's open portfolio
 *
 * Used to retrieve a paginated list of pools where the user has open (active) positions.
 * Supports sorting by various metrics and pagination.
 */
export type GetOpenPortfolioQuery = {
    /**
     * Page number for pagination (minimum: 1, default: 1)
     */
    page?: number;
    /**
     * Page size for pagination (default: 20, maximum: 50)
     */
    page_size?: number;
    /**
     * Sort by field, default is current_balances
     */
    sort_by?: GetOpenPortfolioSort;
    /**
     * Sort direction, default is DESC
     */
    sort_direction?: SortDirection;
    /**
     * User's wallet address
     */
    user: string;
};

export type GetOpenPortfolioResponse = {
    hasNext: boolean;
    page: number;
    pageSize: number;
    pools: Array<PoolOpenPortfolioItem>;
    solPrice?: string | null;
    total?: null | TotalMetrics;
    totalCount: number;
    /**
     * Total number of open positions across all pools
     */
    totalPositions: number;
};

export type GetOpenPortfolioSort = 'current_balances' | 'unclaimed_fee' | 'fee_per_tvl24h';

export type GetPoolParams = {
    /**
     * Base58-encoded pool address
     */
    address: string;
};

/**
 * Query parameters for getting position PnL data
 */
export type GetPoolPositionPnLQuery = {
    /**
     * Page number for pagination (minimum: 1, default: 1)
     */
    page?: number;
    /**
     * Page size for pagination (minimum: 1, maximum: 100, default: 20)
     */
    page_size?: number;
    /**
     * Filter positions by status: "open", "closed", or "all" (default: "all")
     */
    status?: PositionStatus;
    /**
     * The user's wallet address
     */
    user: string;
};

export type GetPoolPositionPnLResponse = {
    hasNext: boolean;
    page: number;
    pageSize: number;
    positions: Array<PositionPnLData>;
    rewardTokenX?: string | null;
    rewardTokenXPrice: string;
    rewardTokenY?: string | null;
    rewardTokenYPrice: string;
    solPrice?: string | null;
    tokenX?: string | null;
    tokenXPrice: string;
    tokenY?: string | null;
    tokenYPrice: string;
    totalCount: number;
};

export type GetPoolsQuery = {
    /**
     * Conditions to filter documents by field values
     *
     * Format: `<expr> [&& <expr> ...]`
     *
     * Where each expression is: `<field><op><value>`
     *
     * Allowed fields:
     * - Numeric: `tvl` `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
     * - Boolean: `is_blacklisted`
     * - Text: `pool_address` `name` `token_x` `token_y`
     *
     * Operators:
     * - Numeric: `=` `>` `>=` `<` `<=`
     * - Boolean: `=true` `=false`
     * - Text:
     * - exact match: `=<value>`
     * - multi-value OR: `=[value1|value2|...]`
     *
     * Notes:
     * - Multiple expressions are combined using logical **AND** (`&&`)
     * - Whitespace around operators is ignored
     *
     * Examples:
     * - `tvl>1000`
     * - `is_blacklisted=false && volume_24h>=50000`
     */
    filter_by?: string | null;
    /**
     * Page number (1-based)
     */
    page?: number | null;
    /**
     * Number of pools to return per page. Max 1000
     */
    page_size?: number | null;
    /**
     * Search query used to match pools by name, tokens, or address
     */
    query?: string | null;
    /**
     * Sort results by one or more fields
     *
     * Format:
     * - Time-windowed metrics: `<metric>_<window>:<direction>`
     * - Non-windowed metrics: `<field>:<direction>`
     *
     * - `direction`: `asc` or `desc`
     * - `window` (when applicable): `5m` `30m` `1h` `2h` `4h` `12h` `24h`
     *
     * Available fields:
     * - Time-windowed metrics: `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
     * - Non-windowed metrics: `tvl` `fee_pct` `bin_step` `pool_created_at` `farm_apy`
     *
     * Default: `volume_24h:desc`
     *
     * Examples:
     * - `volume_24h:desc`
     * - `fee_1h:asc`
     * - `tvl:desc`
     */
    sort_by?: string | null;
};

/**
 * Query parameters for getting user portfolio
 */
export type GetPortfolioQuery = {
    /**
     * Only include pools with positions closed within this many days
     * Applied only for closed positions
     * (minimum: 1, maximum: 365, default: 120)
     */
    days_back?: number;
    /**
     * Page number for pagination (minimum: 1, default: 1)
     */
    page?: number;
    /**
     * Page size for pagination (default: 20, maximum: 50)
     */
    page_size?: number;
    /**
     * User's wallet address
     */
    user: string;
};

/**
 * User portfolio response with aggregated pool data
 */
export type GetPortfolioResponse = {
    hasNext: boolean;
    page: number;
    pageSize: number;
    pools: Array<PoolPortfolioItem>;
    totalCount: number;
    /**
     * Total number of closed positions across all pools
     */
    totalPositions: number;
};

/**
 * Query parameters for getting portfolio totals
 */
export type GetPortfolioTotalQuery = {
    /**
     * User's wallet address
     */
    user: string;
};

/**
 * Query parameters for getting historical events for a position
 */
export type GetPositionHistoricalEventsQuery = {
    event_type?: null | PositionEventType;
    order_direction?: null | PositionEventOrderDirection;
};

export type GetPositionHistoricalEventsResponse = {
    events: Array<PositionEvent>;
};

export type GetWalletPoolAccumulatedFeesParams = {
    /**
     * Base58-encoded pool address
     */
    pool_address: string;
    /**
     * Base58-encoded wallet address
     */
    wallet: string;
};

export type GetWalletTotalClaimsResponse = {
    fee_claim_count: number;
    last_fee_claim_time?: string | null;
    last_reward_claim_time?: string | null;
    pool_address: string;
    reward_claim_count: number;
    total_claims_sol: string;
    total_claims_usd: string;
    total_fee_x: string;
    total_fee_x_sol: string;
    total_fee_x_usd: string;
    total_fee_y: string;
    total_fee_y_sol: string;
    total_fee_y_usd: string;
    total_reward_x: string;
    total_reward_x_sol: string;
    total_reward_x_usd: string;
    total_reward_y: string;
    total_reward_y_sol: string;
    total_reward_y_usd: string;
    user_address: string;
};

export type GroupResponse = {
    group_name: string;
    has_farm: boolean;
    lexical_order_mints: string;
    max_fee_tvl_ratio: number;
    pool_count: number;
    token_x: string;
    token_y: string;
    total_tvl: number;
    total_volume: number;
};

export type LimitOrderBinFillStatus = 'not_filled' | 'partial_filled' | 'fulfilled';

/**
 * Pool-level metadata shared across all limit-order responses for a given
 * pool. Same shape on both the open- and closed-order endpoints so clients
 * can render the pool header from a single struct.
 */
export type LimitOrderPoolDetails = {
    /**
     * Pool base fee as a percentage (f64 serialized as string to preserve precision).
     */
    base_fee: string;
    /**
     * Pool bin step in basis points (on-chain u16).
     */
    bin_step: number;
    /**
     * 0 = `InputOnly` (bonus flows to the deposit/input token),
     * 1 = `OnlyY` (bonus always in token Y regardless of side).
     */
    collect_fee_mode: number;
    /**
     * `"{X}-{Y}"` computed from token symbols.
     */
    pair_name: string;
    pool_address: string;
    token_x: string;
    token_x_icon: string;
    token_x_mint: string;
    token_y: string;
    token_y_icon: string;
    token_y_mint: string;
};

/**
 * Summary totals for a user's limit orders across every pool. `_sol` variants
 * follow two different time bases:
 * - `total_deposit_sol` — SOL value at *placement time* (SOL price frozen when
 * the PlaceLimitOrderEvt was indexed).
 * - `total_bonus_sol` — SOL value at *current spot* (current SOL price applied
 * to the running bonus accrual).
 *
 * "Open" means placed with no row in `close_limit_orders`. "Closed" means an
 * `close_limit_orders` row exists. Cancel events are not terminal — a
 * cancelled-but-not-closed order is still open.
 */
export type LimitOrderSummaryResponse = {
    /**
     * Number of limit orders that have been closed on-chain.
     */
    closed_orders: number;
    /**
     * Number of limit orders that are still open (no close event).
     */
    open_orders: number;
    /**
     * Total SOL value of bonus accrued across open orders (current spot SOL price).
     */
    total_bonus_sol: string;
    /**
     * Total USD value of bonus (pro-rata fee share) accrued across open orders.
     */
    total_bonus_usd: string;
    /**
     * Total SOL value of currently-open order deposits (placement-time SOL price).
     */
    total_deposit_sol: string;
    /**
     * Total USD value of currently-open order deposits.
     */
    total_deposit_usd: string;
};

/**
 * Query parameters for paginated limit order endpoints.
 */
export type LimitOrdersPaginationQuery = {
    /**
     * Page number, 1-indexed. Default: 1
     */
    page?: number | null;
    /**
     * Number of results per page. Default: 20, max: 1000
     */
    page_size?: number | null;
};

/**
 * Path parameter for wallet-only `/wallets/{wallet}/limit_orders*` endpoints.
 */
export type LimitOrdersWalletPath = {
    /**
     * Base58-encoded wallet address
     */
    wallet: string;
};

/**
 * Path parameters for pool-scoped endpoints
 * `/wallets/{wallet}/limit_orders/{open|closed}/{pool_address}`.
 */
export type LimitOrdersWalletPoolPath = {
    /**
     * Base58-encoded LB pair (pool) address
     */
    pool_address: string;
    /**
     * Base58-encoded wallet address
     */
    wallet: string;
};

export type OhlcvResponse = {
    close: number;
    high: number;
    low: number;
    open: number;
    timestamp: number;
    timestamp_str: string;
    volume: number;
};

/**
 * Per-bin point in an open limit order's distribution. Amounts are in the
 * input (deposit) token's display units — token X for an ask, token Y for a
 * bid — matching the order-level `filled_input_amount`.
 */
export type OpenLimitOrderBinPoint = {
    bin_id: number;
    /**
     * Original deposit at this bin in input-token display units. The
     * frontend's bar-chart heights are derived from `deposit_amount × price`
     * (i.e. the notional in output-token terms) — no per-bin USD field is
     * exposed since order-level totals carry the dollar/SOL values.
     */
    deposit_amount: string;
    fill_status: LimitOrderBinFillStatus;
    /**
     * Filled portion in input-token display units.
     */
    fulfilled_amount: string;
    /**
     * Output-token amount received from this bin's fills, in output-token
     * display units. Ask: `fulfilled_amount × price`. Bid: `fulfilled_amount /
     * price`.
     */
    output_received_amount: string;
    /**
     * Price at this bin (token Y per token X).
     */
    price: string;
    /**
     * Remaining unfilled portion in input-token display units.
     */
    unfilled_amount: string;
};

/**
 * Per-order detail for a live limit order inside a specific pool. Amounts use
 * the `input`/`output` terminology (input is the deposited token, output is
 * the expected fill token) to abstract ask/bid semantics for the frontend.
 * Pool-level metadata (pair, bin step, fees, tokens, current pool state) lives
 * once at the top of the response — see `OpenLimitOrdersForPoolResponse`.
 */
export type OpenLimitOrderDetail = {
    /**
     * Per-bin distribution sorted by `bin_id` ASC.
     */
    bin_distribution: Array<OpenLimitOrderBinPoint>;
    /**
     * Filled amount expressed in input (deposit) token units.
     */
    filled_input_amount: string;
    filled_pct: string;
    input_amount: string;
    input_amount_sol: string;
    input_amount_usd: string;
    input_token: string;
    input_token_mint: string;
    /**
     * true = ask (selling X for Y), false = bid (buying X with Y).
     */
    is_ask_side: boolean;
    limit_order_address: string;
    lower_pool_price: number;
    /**
     * Bin id of the nearest still-unfilled (or partially-filled) bin to the
     * current active bin. `None` only when every bin in the order is
     * fulfilled. Tie-broken to the lower `bin_id`.
     */
    nearest_unfilled_bin_id?: number | null;
    /**
     * Price of `nearest_unfilled_bin_id` (same units as `current_pool_price`
     * on the enclosing response wrapper). `None` only when fully filled.
     * Frontend computes the percentage distance as
     * `(nearest_unfilled_bin_price - current_pool_price) / current_pool_price`.
     */
    nearest_unfilled_bin_price?: string | null;
    /**
     * Unix seconds.
     */
    opened_at: number;
    opened_at_signature: string;
    opened_at_slot: number;
    /**
     * Expected output at placement bin prices — `amount_y` for asks,
     * `amount_x` for bids. Pre-computed on the
     * `limit_orders_aggregate.output_amount_expected` column by the open
     * MV via `if(is_ask_side, amount_y, amount_x)`, so this field is just
     * the stored value (no per-bin math at request time).
     */
    output_amount_expected: string;
    output_token: string;
    output_token_mint: string;
    /**
     * SOL value of bonus (current spot SOL price).
     */
    total_bonus_sol: string;
    total_bonus_usd: string;
    total_bonus_x: string;
    total_bonus_y: string;
    /**
     * Total filled amount in *output* token display units — what the user has
     * received. Computed as `Σ fulfilled_at_bin × bin_price` for asks, or
     * `Σ fulfilled / bin_price` for bids. Each bin's price is fixed by the
     * program at fill time, so this is the close estimate of the on-chain
     * output (a true figure is only emitted at cancel).
     */
    total_filled_amount: string;
    total_filled_amount_sol: string;
    total_filled_amount_usd: string;
    /**
     * Total unfilled deposit in input-token display units
     * (`input_amount - filled_input_amount`).
     */
    total_unfilled_amount: string;
    total_unfilled_amount_sol: string;
    total_unfilled_amount_usd: string;
    upper_pool_price: number;
    user_address: string;
};

/**
 * Pool-grouped summary of a user's live limit orders within a single pool.
 * `fully_filled_orders` counts orders whose summed `fulfilled_amount` across
 * bins has reached the summed `lo_amount` (deposit-amount weighted, matching
 * the per-order `filled_pct == 100`).
 */
export type OpenLimitOrderPoolSummary = {
    filled_pct: string;
    fully_filled_orders: number;
    pool: LimitOrderPoolDetails;
    /**
     * SOL value of bonus (current spot SOL price).
     */
    total_bonus_sol: string;
    total_bonus_usd: string;
    total_bonus_x: string;
    total_bonus_y: string;
    /**
     * SOL value of deposits, deposit-side only (placement-time SOL price).
     */
    total_deposit_sol: string;
    /**
     * USD value of deposits, deposit-side only (an ask deposits X, a bid deposits Y).
     */
    total_deposit_usd: string;
    total_deposit_x: string;
    /**
     * SOL value of token X deposits (placement-time SOL price).
     */
    total_deposit_x_sol: string;
    /**
     * USD value of token X deposits (placement-time price; only ask orders deposit X).
     */
    total_deposit_x_usd: string;
    total_deposit_y: string;
    /**
     * SOL value of token Y deposits (placement-time SOL price).
     */
    total_deposit_y_sol: string;
    /**
     * USD value of token Y deposits (placement-time price; only bid orders deposit Y).
     */
    total_deposit_y_usd: string;
    total_orders: number;
    /**
     * SOL value of withdrawals (cancel-event-time SOL price).
     */
    total_withdrawal_sol: string;
    total_withdrawal_usd: string;
    total_withdrawal_x: string;
    /**
     * SOL value of token X withdrawn (cancel-event-time SOL price).
     */
    total_withdrawal_x_sol: string;
    /**
     * USD value of token X withdrawn from cancel events (cancel-event-time USD price).
     */
    total_withdrawal_x_usd: string;
    total_withdrawal_y: string;
    /**
     * SOL value of token Y withdrawn (cancel-event-time SOL price).
     */
    total_withdrawal_y_sol: string;
    /**
     * USD value of token Y withdrawn from cancel events (cancel-event-time USD price).
     */
    total_withdrawal_y_usd: string;
};

/**
 * Top-level response for `GET /wallets/{wallet}/limit_orders/open/pools/{pool_address}`.
 * Pool metadata and live pool state are emitted once at the top; per-order
 * rows in `data` only carry order-specific fields.
 */
export type OpenLimitOrdersForPoolResponse = {
    /**
     * Live active bin id from the dynamic pool cache.
     */
    current_active_bin_id: number;
    current_page: number;
    /**
     * Pool's live price (token Y per token X) from the dynamic pool cache.
     */
    current_pool_price: string;
    data: Array<OpenLimitOrderDetail>;
    page_size: number;
    pages: number;
    pool: LimitOrderPoolDetails;
    total: number;
};

export type OpenPosition = {
    created_at: number;
    current_position: CurrentPosition;
    lower_bin_id: number;
    lower_bin_price: number;
    pool_address: string;
    position_address: string;
    total_claimed_fees: AmountTotals;
    total_deposits: AmountTotals;
    total_withdraws: AmountTotals;
    unrealized_pnl: number;
    unrealized_pnl_change_pct: number;
    upper_bin_id: number;
    upper_bin_price: number;
};

export type OpenPositionsResponse = {
    data: Array<PositionsByPool>;
    total_pools: number;
    total_positions: number;
};

export type PaginationResponseClosedLimitOrderPoolSummary = {
    current_page: number;
    data: Array<{
        filled_pct: string;
        fully_filled_orders: number;
        /**
         * Unix seconds. Latest close-event block time for this pool.
         */
        last_closed_at: number;
        pool: LimitOrderPoolDetails;
        /**
         * SOL value of bonus (cancel-event-time SOL price).
         */
        total_bonus_sol: string;
        total_bonus_usd: string;
        total_bonus_x: string;
        /**
         * SOL value of token X bonus (cancel-event-time SOL price).
         */
        total_bonus_x_sol: string;
        /**
         * USD value of token X bonus (cancel-event-time USD price).
         */
        total_bonus_x_usd: string;
        total_bonus_y: string;
        /**
         * SOL value of token Y bonus (cancel-event-time SOL price).
         */
        total_bonus_y_sol: string;
        /**
         * USD value of token Y bonus (cancel-event-time USD price).
         */
        total_bonus_y_usd: string;
        /**
         * SOL value of deposits, deposit-side only (placement-time SOL price).
         */
        total_deposit_sol: string;
        /**
         * USD value of deposits, deposit-side only (an ask deposits X, a bid deposits Y).
         */
        total_deposit_usd: string;
        total_deposit_x: string;
        /**
         * SOL value of token X deposits (placement-time SOL price).
         */
        total_deposit_x_sol: string;
        /**
         * USD value of token X deposits (placement-time price; only ask orders deposit X).
         */
        total_deposit_x_usd: string;
        total_deposit_y: string;
        /**
         * SOL value of token Y deposits (placement-time SOL price).
         */
        total_deposit_y_sol: string;
        /**
         * USD value of token Y deposits (placement-time price; only bid orders deposit Y).
         */
        total_deposit_y_usd: string;
        total_orders: number;
        /**
         * SOL value of withdrawals (cancel-event-time SOL price).
         */
        total_withdrawal_sol: string;
        total_withdrawal_usd: string;
        total_withdrawal_x: string;
        /**
         * SOL value of token X withdrawn (cancel-event-time SOL price).
         */
        total_withdrawal_x_sol: string;
        /**
         * USD value of token X withdrawn (cancel-event-time USD price).
         */
        total_withdrawal_x_usd: string;
        total_withdrawal_y: string;
        /**
         * SOL value of token Y withdrawn (cancel-event-time SOL price).
         */
        total_withdrawal_y_sol: string;
        /**
         * USD value of token Y withdrawn (cancel-event-time USD price).
         */
        total_withdrawal_y_usd: string;
    }>;
    page_size: number;
    pages: number;
    total: number;
};

export type PaginationResponseGroupResponse = {
    current_page: number;
    data: Array<{
        group_name: string;
        has_farm: boolean;
        lexical_order_mints: string;
        max_fee_tvl_ratio: number;
        pool_count: number;
        token_x: string;
        token_y: string;
        total_tvl: number;
        total_volume: number;
    }>;
    page_size: number;
    pages: number;
    total: number;
};

export type PaginationResponseOpenLimitOrderPoolSummary = {
    current_page: number;
    data: Array<{
        filled_pct: string;
        fully_filled_orders: number;
        pool: LimitOrderPoolDetails;
        /**
         * SOL value of bonus (current spot SOL price).
         */
        total_bonus_sol: string;
        total_bonus_usd: string;
        total_bonus_x: string;
        total_bonus_y: string;
        /**
         * SOL value of deposits, deposit-side only (placement-time SOL price).
         */
        total_deposit_sol: string;
        /**
         * USD value of deposits, deposit-side only (an ask deposits X, a bid deposits Y).
         */
        total_deposit_usd: string;
        total_deposit_x: string;
        /**
         * SOL value of token X deposits (placement-time SOL price).
         */
        total_deposit_x_sol: string;
        /**
         * USD value of token X deposits (placement-time price; only ask orders deposit X).
         */
        total_deposit_x_usd: string;
        total_deposit_y: string;
        /**
         * SOL value of token Y deposits (placement-time SOL price).
         */
        total_deposit_y_sol: string;
        /**
         * USD value of token Y deposits (placement-time price; only bid orders deposit Y).
         */
        total_deposit_y_usd: string;
        total_orders: number;
        /**
         * SOL value of withdrawals (cancel-event-time SOL price).
         */
        total_withdrawal_sol: string;
        total_withdrawal_usd: string;
        total_withdrawal_x: string;
        /**
         * SOL value of token X withdrawn (cancel-event-time SOL price).
         */
        total_withdrawal_x_sol: string;
        /**
         * USD value of token X withdrawn from cancel events (cancel-event-time USD price).
         */
        total_withdrawal_x_usd: string;
        total_withdrawal_y: string;
        /**
         * SOL value of token Y withdrawn (cancel-event-time SOL price).
         */
        total_withdrawal_y_sol: string;
        /**
         * USD value of token Y withdrawn from cancel events (cancel-event-time USD price).
         */
        total_withdrawal_y_usd: string;
    }>;
    page_size: number;
    pages: number;
    total: number;
};

export type PaginationResponsePoolResponse = {
    current_page: number;
    data: Array<{
        /**
         * Address of the liquidity pair
         */
        address: string;
        /**
         * 24 hour APR
         */
        apr: number;
        /**
         * 24 hour APY
         */
        apy: number;
        /**
         * Pool created at timestamp
         */
        created_at: number;
        /**
         * Cumulative metrics
         */
        cumulative_metrics: CumulativeMetrics;
        /**
         * Price of the liquidity pair
         */
        current_price: number;
        /**
         * Dynamic fee rate. Which is equal to base fee + variable fee.
         */
        dynamic_fee_pct: number;
        /**
         * Farm reward apr
         */
        farm_apr: number;
        /**
         * Farm reward apy
         */
        farm_apy: number;
        /**
         * Fee TVL ratio in percentage in different timeframes
         */
        fee_tvl_ratio: TimeWindowData;
        /**
         * Fee data in different timeframes
         */
        fees: TimeWindowData;
        /**
         * Whether the pool has a farm or not
         */
        has_farm: boolean;
        /**
         * Flag to indicate whether the pair is blacklisted
         */
        is_blacklisted: boolean;
        /**
         * Launchpad of the pair
         */
        launchpad?: string | null;
        /**
         * Name of the liquidity pair
         */
        name: string;
        /**
         * Pool config
         */
        pool_config: PoolConfig;
        /**
         * Protocol fee data in different timeframes
         */
        protocol_fees: TimeWindowData;
        /**
         * Address of token X reserve of the liquidity pair
         */
        reserve_x: string;
        /**
         * Address of token Y reserve of the liquidity pair
         */
        reserve_y: string;
        /**
         * Address of the farming reward X of the liquidity pair
         */
        reward_mint_x: string;
        /**
         * Address of the farming reward Y of the liquidity pair
         */
        reward_mint_y: string;
        /**
         * Tags of the pair
         */
        tags: Array<string>;
        /**
         * Token X of the liquidity pair
         */
        token_x: TokenMetrics;
        /**
         * Token X amount the liquidity pair hold
         */
        token_x_amount: number;
        /**
         * Token Y of the liquidity pair
         */
        token_y: TokenMetrics;
        /**
         * Token Y amount the liquidity pair hold
         */
        token_y_amount: number;
        /**
         * Total liquidity the liquidity pair holding. Also known as Total Value Locked
         */
        tvl: number;
        /**
         * Volume data in different timeframes
         */
        volume: TimeWindowData;
    }>;
    page_size: number;
    pages: number;
    total: number;
};

export type PoolAddressPath = {
    pool_address: string;
};

export type PoolConfig = {
    /**
     * Base fee rate
     */
    base_fee_pct: number;
    /**
     * Bin step of the pool
     */
    bin_step: number;
    /**
     * Fee collection mode
     */
    collect_fee_mode: number;
    /**
     * Maximum fee rate
     */
    max_fee_pct: number;
    /**
     * Protocol fee rate. A cut from trade fee.
     */
    protocol_fee_pct: number;
};

/**
 * Aggregated portfolio data for a single pool
 */
export type PoolOpenPortfolioItem = {
    /**
     * Current balance in USD
     */
    balances: string;
    /**
     * Current balance in SOL
     */
    balancesSol?: string | null;
    baseFee: number;
    binStep: number;
    collectFeeMode: number;
    /**
     * User's fee per tvl in rolling 24h
     */
    feePerTvl24h: string;
    /**
     * List of position addresses in pool
     */
    listPositions: Array<string>;
    /**
     * Number of open positions
     */
    openPositionCount: number;
    /**
     * Pool dynamic fields
     * Pool is out of range if one of the user's positions is out of range
     * If this is None, it means we couldn't determine out_of_range status
     */
    outOfRange?: boolean | null;
    /**
     * Live PnL
     */
    pnl: string;
    /**
     * Live PnL percentage change
     */
    pnlPctChange: string;
    /**
     * Live PnL in SOL
     */
    pnlSol?: string | null;
    /**
     * Live PnL percentage change (SOL)
     */
    pnlSolPctChange?: string | null;
    poolAddress: string;
    /**
     * Current pool price
     */
    poolPrice?: number | null;
    poolStateUpdatedAtBlockTime?: number | null;
    /**
     * Tracing metrics
     */
    poolStateUpdatedAtSlot?: number | null;
    /**
     * List of position addresses that are out of range
     */
    positionsOutOfRange: Array<string>;
    rewardX: string;
    rewardY: string;
    tokenX: string;
    tokenXIcon: string;
    tokenXMint: string;
    tokenY: string;
    tokenYIcon: string;
    tokenYMint: string;
    /**
     * Total deposit in USD
     */
    totalDeposit: string;
    /**
     * Total deposit in SOL (historical per-event SOL price)
     */
    totalDepositSol?: string | null;
    /**
     * Current unclaimed fee in USD
     */
    unclaimedFees: string;
    /**
     * Current unclaimed fee in SOL
     */
    unclaimedFeesSol?: string | null;
};

/**
 * Aggregated portfolio data for a single pool
 */
export type PoolPortfolioItem = {
    baseFee: string;
    binStep: string;
    collectFeeMode: number;
    lastClosedAt?: number | null;
    /**
     * PnL percent change (USD)
     */
    pnlPctChange: string;
    /**
     * Total PnL in SOL (historical per-event SOL price)
     */
    pnlSol: string;
    /**
     * PnL percent change (SOL)
     */
    pnlSolPctChange: string;
    /**
     * Total PnL in USD, including realized and unrealized
     */
    pnlUsd: string;
    poolAddress: string;
    tokenX: string;
    tokenXIcon: string;
    tokenXMint: string;
    tokenY: string;
    tokenYIcon: string;
    tokenYMint: string;
    /**
     * Total deposit in USD
     */
    totalDeposit: string;
    /**
     * Total deposit in SOL (historical per-event SOL price)
     */
    totalDepositSol: string;
    totalDepositTokenX: string;
    totalDepositTokenXSol: string;
    totalDepositTokenXUsd: string;
    totalDepositTokenY: string;
    totalDepositTokenYSol: string;
    totalDepositTokenYUsd: string;
    /**
     * Total claim fee in USD
     */
    totalFee: string;
    /**
     * Total claim fee in SOL (historical per-event SOL price)
     */
    totalFeeSol: string;
    totalFeeTokenX: string;
    totalFeeTokenXSol: string;
    totalFeeTokenXUsd: string;
    totalFeeTokenY: string;
    totalFeeTokenYSol: string;
    totalFeeTokenYUsd: string;
    /**
     * Total withdrawal in USD
     */
    totalWithdrawal: string;
    /**
     * Total withdrawal in SOL (historical per-event SOL price)
     */
    totalWithdrawalSol: string;
    totalWithdrawalTokenX: string;
    totalWithdrawalTokenXSol: string;
    totalWithdrawalTokenXUsd: string;
    totalWithdrawalTokenY: string;
    totalWithdrawalTokenYSol: string;
    totalWithdrawalTokenYUsd: string;
};

export type PoolResponse = {
    /**
     * Address of the liquidity pair
     */
    address: string;
    /**
     * 24 hour APR
     */
    apr: number;
    /**
     * 24 hour APY
     */
    apy: number;
    /**
     * Pool created at timestamp
     */
    created_at: number;
    /**
     * Cumulative metrics
     */
    cumulative_metrics: CumulativeMetrics;
    /**
     * Price of the liquidity pair
     */
    current_price: number;
    /**
     * Dynamic fee rate. Which is equal to base fee + variable fee.
     */
    dynamic_fee_pct: number;
    /**
     * Farm reward apr
     */
    farm_apr: number;
    /**
     * Farm reward apy
     */
    farm_apy: number;
    /**
     * Fee TVL ratio in percentage in different timeframes
     */
    fee_tvl_ratio: TimeWindowData;
    /**
     * Fee data in different timeframes
     */
    fees: TimeWindowData;
    /**
     * Whether the pool has a farm or not
     */
    has_farm: boolean;
    /**
     * Flag to indicate whether the pair is blacklisted
     */
    is_blacklisted: boolean;
    /**
     * Launchpad of the pair
     */
    launchpad?: string | null;
    /**
     * Name of the liquidity pair
     */
    name: string;
    /**
     * Pool config
     */
    pool_config: PoolConfig;
    /**
     * Protocol fee data in different timeframes
     */
    protocol_fees: TimeWindowData;
    /**
     * Address of token X reserve of the liquidity pair
     */
    reserve_x: string;
    /**
     * Address of token Y reserve of the liquidity pair
     */
    reserve_y: string;
    /**
     * Address of the farming reward X of the liquidity pair
     */
    reward_mint_x: string;
    /**
     * Address of the farming reward Y of the liquidity pair
     */
    reward_mint_y: string;
    /**
     * Tags of the pair
     */
    tags: Array<string>;
    /**
     * Token X of the liquidity pair
     */
    token_x: TokenMetrics;
    /**
     * Token X amount the liquidity pair hold
     */
    token_x_amount: number;
    /**
     * Token Y of the liquidity pair
     */
    token_y: TokenMetrics;
    /**
     * Token Y amount the liquidity pair hold
     */
    token_y_amount: number;
    /**
     * Total liquidity the liquidity pair holding. Also known as Total Value Locked
     */
    tvl: number;
    /**
     * Volume data in different timeframes
     */
    volume: TimeWindowData;
};

export type PoolsSortBy = 'tvl' | 'volume24h' | 'fee24h';

/**
 * Portfolio total PnL response
 */
export type PortfolioTotalResponse = {
    /**
     * Total PnL percentage change across all pools (USD)
     */
    totalPnlPctChange: string;
    /**
     * Total PnL in SOL across all pools (historical per-event SOL price)
     */
    totalPnlSol: string;
    /**
     * Total PnL percentage change across all pools (SOL)
     */
    totalPnlSolPctChange: string;
    /**
     * Total amount deposited in USD across all pools
     * NOTE: Commented out for now, but may be needed in the future
     * Total PnL in USD across all pools
     */
    totalPnlUsd: string;
};

export type PositionEvent = {
    amountX: string;
    amountXUsd: string;
    amountY: string;
    amountYUsd: string;
    blockTime: number;
    createdAt: string;
    eventType: string;
    ixIndex: number;
    poolAddress: string;
    positionAddress: string;
    signature: string;
    slot: number;
    tokenX: string;
    tokenY: string;
    totalUsd: string;
    userAddress: string;
};

export type PositionEventOrderDirection = 'asc' | 'desc';

export type PositionEventType = 'add' | 'remove' | 'claim_fee' | 'claim_reward';

export type PositionPnLData = {
    allTimeDeposits: TokenPairWithTotal;
    allTimeFees: TokenPairWithTotal;
    allTimeWithdrawals: TokenPairWithTotal;
    closedAt?: number | null;
    createdAt?: number | null;
    /**
     * User's fee per tvl in rolling 24h
     */
    feePerTvl24h: string;
    isClosed: boolean;
    isOutOfRange?: boolean | null;
    lowerBinId: number;
    maxPrice: string;
    minPrice: string;
    pnlPctChange: string;
    pnlSol?: number | null;
    pnlSolPctChange?: number | null;
    pnlUsd: string;
    poolActiveBinId?: number | null;
    poolActivePrice?: string | null;
    positionAddress: string;
    unrealizedPnl?: null | UnrealizedPnL;
    upperBinId: number;
};

export type PositionStatus = 'open' | 'closed' | 'all';

export type PositionsByPool = {
    active_bin_id: number;
    bin_step: number;
    collect_fee_mode: number;
    fee_pct: number;
    name: string;
    pool_address: string;
    pool_price?: number | null;
    positions: Array<OpenPosition>;
    token_x: Token;
    token_y: Token;
};

export type ProtocolMetricsResponse = {
    fee_24h: number;
    total_fees: number;
    total_pools: number;
    total_tvl: number;
    total_volume: number;
    volume_24h: number;
};

export type SortDirection = 'asc' | 'desc';

export type TimeFrame = '5m' | '30m' | '1h' | '2h' | '4h' | '12h' | '24h';

export type TimeWindowData = {
    '12h': number;
    '1h': number;
    '24h': number;
    '2h': number;
    '30m': number;
    '4h': number;
};

export type TimeseriesResponseOhlcvResponse = {
    data: Array<{
        close: number;
        high: number;
        low: number;
        open: number;
        timestamp: number;
        timestamp_str: string;
        volume: number;
    }>;
    end_time: number;
    start_time: number;
    timeframe?: string | null;
};

export type TimeseriesResponseVolumeHistoryResponse = {
    data: Array<{
        fees: number;
        protocol_fees: number;
        timestamp: number;
        timestamp_str: string;
        volume: number;
    }>;
    end_time: number;
    start_time: number;
    timeframe?: string | null;
};

export type Token = {
    address: string;
    decimals: number;
    icon: string;
    name: string;
    symbol: string;
};

export type TokenAmount = {
    amount: string;
    amountSol?: string | null;
    usd: string;
};

export type TokenMetrics = {
    address: string;
    decimals: number;
    freeze_authority_disabled: boolean;
    holders: number;
    is_verified: boolean;
    market_cap: number;
    name: string;
    price: number;
    symbol: string;
    total_supply: number;
};

export type TokenPairWithTotal = {
    tokenX: TokenAmount;
    tokenY: TokenAmount;
    total: TotalUsd;
};

/**
 * Total metrics related to user portfolio
 */
export type TotalMetrics = {
    /**
     * Current balance in USD
     */
    balances: string;
    /**
     * Current balance in SOL
     */
    balancesSol?: string | null;
    /**
     * Current pnl in USD
     */
    pnl: string;
    /**
     * PnL percent change (USD)
     */
    pnlPctChange: string;
    /**
     * Current pnl in SOL
     */
    pnlSol?: string | null;
    /**
     * PnL percent change (SOL)
     */
    pnlSolPctChange?: string | null;
    /**
     * Total number of open positions across all pools
     */
    totalPositions: number;
    /**
     * Current unclaimed fees in USD
     */
    unclaimedFees: string;
    /**
     * Current unclaimed fees in SOL
     */
    unclaimedFeesSol?: string | null;
};

export type TotalUsd = {
    sol?: string | null;
    usd: string;
};

export type UnrealizedPnL = {
    balanceTokenX: TokenAmount;
    balanceTokenY: TokenAmount;
    /**
     * Total current position balances in USD
     */
    balances: number;
    /**
     * Total current position balances in SOL
     */
    balancesSol?: string | null;
    unclaimedFeeTokenX: TokenAmount;
    unclaimedFeeTokenY: TokenAmount;
    unclaimedRewardTokenX: TokenAmount;
    unclaimedRewardTokenY: TokenAmount;
};

export type VolumeHistoryResponse = {
    fees: number;
    protocol_fees: number;
    timestamp: number;
    timestamp_str: string;
    volume: number;
};

export type GetPoolsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Page number (1-based)
         */
        page?: number | null;
        /**
         * Number of pools to return per page. Max 1000
         */
        page_size?: number | null;
        /**
         * Search query used to match pools by name, tokens, or address
         */
        query?: string | null;
        /**
         * Sort results by one or more fields
         *
         * Format:
         * - Time-windowed metrics: `<metric>_<window>:<direction>`
         * - Non-windowed metrics: `<field>:<direction>`
         *
         * - `direction`: `asc` or `desc`
         * - `window` (when applicable): `5m` `30m` `1h` `2h` `4h` `12h` `24h`
         *
         * Available fields:
         * - Time-windowed metrics: `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
         * - Non-windowed metrics: `tvl` `fee_pct` `bin_step` `pool_created_at` `farm_apy`
         *
         * Default: `volume_24h:desc`
         *
         * Examples:
         * - `volume_24h:desc`
         * - `fee_1h:asc`
         * - `tvl:desc`
         */
        sort_by?: string | null;
        /**
         * Conditions to filter documents by field values
         *
         * Format: `<expr> [&& <expr> ...]`
         *
         * Where each expression is: `<field><op><value>`
         *
         * Allowed fields:
         * - Numeric: `tvl` `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
         * - Boolean: `is_blacklisted`
         * - Text: `pool_address` `name` `token_x` `token_y`
         *
         * Operators:
         * - Numeric: `=` `>` `>=` `<` `<=`
         * - Boolean: `=true` `=false`
         * - Text:
         * - exact match: `=<value>`
         * - multi-value OR: `=[value1|value2|...]`
         *
         * Notes:
         * - Multiple expressions are combined using logical **AND** (`&&`)
         * - Whitespace around operators is ignored
         *
         * Examples:
         * - `tvl>1000`
         * - `is_blacklisted=false && volume_24h>=50000`
         */
        filter_by?: string | null;
    };
    url: '/pools';
};

export type GetPoolsErrors = {
    400: ErrorResponse;
};

export type GetPoolsError = GetPoolsErrors[keyof GetPoolsErrors];

export type GetPoolsResponses = {
    200: {
        current_page: number;
        data: Array<{
            /**
             * Address of the liquidity pair
             */
            address: string;
            /**
             * 24 hour APR
             */
            apr: number;
            /**
             * 24 hour APY
             */
            apy: number;
            /**
             * Pool created at timestamp
             */
            created_at: number;
            /**
             * Cumulative metrics
             */
            cumulative_metrics: CumulativeMetrics;
            /**
             * Price of the liquidity pair
             */
            current_price: number;
            /**
             * Dynamic fee rate. Which is equal to base fee + variable fee.
             */
            dynamic_fee_pct: number;
            /**
             * Farm reward apr
             */
            farm_apr: number;
            /**
             * Farm reward apy
             */
            farm_apy: number;
            /**
             * Fee TVL ratio in percentage in different timeframes
             */
            fee_tvl_ratio: TimeWindowData;
            /**
             * Fee data in different timeframes
             */
            fees: TimeWindowData;
            /**
             * Whether the pool has a farm or not
             */
            has_farm: boolean;
            /**
             * Flag to indicate whether the pair is blacklisted
             */
            is_blacklisted: boolean;
            /**
             * Launchpad of the pair
             */
            launchpad?: string | null;
            /**
             * Name of the liquidity pair
             */
            name: string;
            /**
             * Pool config
             */
            pool_config: PoolConfig;
            /**
             * Protocol fee data in different timeframes
             */
            protocol_fees: TimeWindowData;
            /**
             * Address of token X reserve of the liquidity pair
             */
            reserve_x: string;
            /**
             * Address of token Y reserve of the liquidity pair
             */
            reserve_y: string;
            /**
             * Address of the farming reward X of the liquidity pair
             */
            reward_mint_x: string;
            /**
             * Address of the farming reward Y of the liquidity pair
             */
            reward_mint_y: string;
            /**
             * Tags of the pair
             */
            tags: Array<string>;
            /**
             * Token X of the liquidity pair
             */
            token_x: TokenMetrics;
            /**
             * Token X amount the liquidity pair hold
             */
            token_x_amount: number;
            /**
             * Token Y of the liquidity pair
             */
            token_y: TokenMetrics;
            /**
             * Token Y amount the liquidity pair hold
             */
            token_y_amount: number;
            /**
             * Total liquidity the liquidity pair holding. Also known as Total Value Locked
             */
            tvl: number;
            /**
             * Volume data in different timeframes
             */
            volume: TimeWindowData;
        }>;
        page_size: number;
        pages: number;
        total: number;
    };
};

export type GetPoolsResponse = GetPoolsResponses[keyof GetPoolsResponses];

export type GetPoolsGroupsData = {
    body?: never;
    path?: never;
    query?: {
        /**
         * Page number (1-based)
         */
        page?: number | null;
        /**
         * Number of pools to return per page. Max 100
         */
        page_size?: number | null;
        /**
         * Search query used to match pools by name, tokens, or address
         */
        query?: string | null;
        /**
         * Sort results by one or more fields
         *
         * Format:
         * - Time-windowed metrics: `<metric>_<window>:<direction>`
         * - Non-windowed metrics: `<field>:<direction>`
         *
         * - `direction`: `asc` or `desc`
         * - `window` (when applicable): `5m` `30m` `1h` `2h` `4h` `12h` `24h`
         *
         * Available fields:
         * - Time-windowed metrics: `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
         * - Non-windowed metrics: `tvl` `fee_pct` `bin_step` `pool_created_at` `farm_apy`
         *
         * Default: `volume_24h:desc`
         *
         * Examples:
         * - `volume_24h:desc`
         * - `fee_1h:asc`
         * - `tvl:desc`
         */
        sort_by?: string | null;
        /**
         * Conditions to filter documents by field values
         *
         * Format: `<expr> [&& <expr> ...]`
         *
         * Where each expression is: `<field><op><value>`
         *
         * Allowed fields:
         * - Numeric: `tvl` `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
         * - Boolean: `is_blacklisted`
         * - Text: `pool_address` `name` `token_x` `token_y`
         *
         * Operators:
         * - Numeric: `=` `>` `>=` `<` `<=`
         * - Boolean: `=true` `=false`
         * - Text:
         * - exact match: `=<value>`
         * - multi-value OR: `=[value1|value2|...]`
         *
         * Notes:
         * - Multiple expressions are combined using logical **AND** (`&&`)
         * - Whitespace around operators is ignored
         *
         * Examples:
         * - `tvl>1000`
         * - `is_blacklisted=false && volume_24h>=50000`
         */
        filter_by?: string | null;
        /**
         * Time window to aggregate volume. Returns sum
         */
        volume_tw?: string | null;
        /**
         * Time window to aggregate fee tvl ratio. Returns Max
         */
        fee_tvl_ratio_tw?: string | null;
    };
    url: '/pools/groups';
};

export type GetPoolsGroupsErrors = {
    400: ErrorResponse;
};

export type GetPoolsGroupsError = GetPoolsGroupsErrors[keyof GetPoolsGroupsErrors];

export type GetPoolsGroupsResponses = {
    200: PaginationResponseGroupResponse;
};

export type GetPoolsGroupsResponse = GetPoolsGroupsResponses[keyof GetPoolsGroupsResponses];

export type GetPoolsGroupsByLexicalOrderMintsData = {
    body?: never;
    path: {
        lexical_order_mints: string;
    };
    query?: {
        /**
         * Page number (1-based)
         */
        page?: number | null;
        /**
         * Number of pools to return per page. Max 100
         */
        page_size?: number | null;
        /**
         * Search query used to match pools by name, tokens, or address
         */
        query?: string | null;
        /**
         * Sort results by one or more fields
         *
         * Format:
         * - Time-windowed metrics: `<metric>_<window>:<direction>`
         * - Non-windowed metrics: `<field>:<direction>`
         *
         * - `direction`: `asc` or `desc`
         * - `window` (when applicable): `5m` `30m` `1h` `2h` `4h` `12h` `24h`
         *
         * Available fields:
         * - Time-windowed metrics: `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
         * - Non-windowed metrics: `tvl` `fee_pct` `bin_step` `pool_created_at` `farm_apy`
         *
         * Default: `volume_24h:desc`
         *
         * Examples:
         * - `volume_24h:desc`
         * - `fee_1h:asc`
         * - `tvl:desc`
         */
        sort_by?: string | null;
        /**
         * Conditions to filter documents by field values
         *
         * Format: `<expr> [&& <expr> ...]`
         *
         * Where each expression is: `<field><op><value>`
         *
         * Allowed fields:
         * - Numeric: `tvl` `volume_*` `fee_*` `fee_tvl_ratio_*` `apr_*`
         * - Boolean: `is_blacklisted`
         * - Text: `pool_address` `name` `token_x` `token_y`
         *
         * Operators:
         * - Numeric: `=` `>` `>=` `<` `<=`
         * - Boolean: `=true` `=false`
         * - Text:
         * - exact match: `=<value>`
         * - multi-value OR: `=[value1|value2|...]`
         *
         * Notes:
         * - Multiple expressions are combined using logical **AND** (`&&`)
         * - Whitespace around operators is ignored
         *
         * Examples:
         * - `tvl>1000`
         * - `is_blacklisted=false && volume_24h>=50000`
         */
        filter_by?: string | null;
    };
    url: '/pools/groups/{lexical_order_mints}';
};

export type GetPoolsGroupsByLexicalOrderMintsErrors = {
    400: ErrorResponse;
};

export type GetPoolsGroupsByLexicalOrderMintsError = GetPoolsGroupsByLexicalOrderMintsErrors[keyof GetPoolsGroupsByLexicalOrderMintsErrors];

export type GetPoolsGroupsByLexicalOrderMintsResponses = {
    200: PaginationResponsePoolResponse;
};

export type GetPoolsGroupsByLexicalOrderMintsResponse = GetPoolsGroupsByLexicalOrderMintsResponses[keyof GetPoolsGroupsByLexicalOrderMintsResponses];

export type GetPoolsByAddressData = {
    body?: never;
    path: {
        /**
         * Base58-encoded pool address
         */
        address: string;
    };
    query?: never;
    url: '/pools/{address}';
};

export type GetPoolsByAddressErrors = {
    400: ErrorResponse;
};

export type GetPoolsByAddressError = GetPoolsByAddressErrors[keyof GetPoolsByAddressErrors];

export type GetPoolsByAddressResponses = {
    200: PoolResponse;
};

export type GetPoolsByAddressResponse = GetPoolsByAddressResponses[keyof GetPoolsByAddressResponses];

export type GetPoolsByAddressOhlcvData = {
    body?: never;
    path: {
        /**
         * Base58-encoded pool address
         */
        address: string;
    };
    query?: {
        /**
         * Candle interval
         *
         * Allowed values `5m` `30m` `1h` `2h` `4h` `12h` `24h`
         *
         * If omitted, the API uses `24h`
         */
        timeframe?: string;
        /**
         * Unix timestamp in seconds (inclusive)
         *
         * If omitted, the API uses a default range based on `timeframe`
         */
        start_time?: number;
        /**
         * Unix timestamp in seconds (inclusive)
         *
         * If omitted, the API uses "now" as the end
         */
        end_time?: number;
    };
    url: '/pools/{address}/ohlcv';
};

export type GetPoolsByAddressOhlcvErrors = {
    400: ErrorResponse;
};

export type GetPoolsByAddressOhlcvError = GetPoolsByAddressOhlcvErrors[keyof GetPoolsByAddressOhlcvErrors];

export type GetPoolsByAddressOhlcvResponses = {
    200: TimeseriesResponseOhlcvResponse;
};

export type GetPoolsByAddressOhlcvResponse = GetPoolsByAddressOhlcvResponses[keyof GetPoolsByAddressOhlcvResponses];

export type GetPoolsByAddressVolumeHistoryData = {
    body?: never;
    path: {
        /**
         * Base58-encoded pool address
         */
        address: string;
    };
    query?: {
        /**
         * Timeframe
         *
         * Allowed values `5m` `30m` `1h` `2h` `4h` `12h` `24h`
         *
         * If omitted, the API uses `24h`
         */
        timeframe?: null | TimeFrame;
        /**
         * Unix timestamp in seconds (inclusive)
         *
         * If omitted, the API uses a default range based on `timeframe`
         */
        start_time?: number;
        /**
         * Unix timestamp in seconds (inclusive)
         *
         * If omitted, the API uses "now" as the end
         */
        end_time?: number;
    };
    url: '/pools/{address}/volume/history';
};

export type GetPoolsByAddressVolumeHistoryErrors = {
    400: ErrorResponse;
};

export type GetPoolsByAddressVolumeHistoryError = GetPoolsByAddressVolumeHistoryErrors[keyof GetPoolsByAddressVolumeHistoryErrors];

export type GetPoolsByAddressVolumeHistoryResponses = {
    200: TimeseriesResponseVolumeHistoryResponse;
};

export type GetPoolsByAddressVolumeHistoryResponse = GetPoolsByAddressVolumeHistoryResponses[keyof GetPoolsByAddressVolumeHistoryResponses];

export type GetPortfolioData = {
    body?: never;
    path?: never;
    query: {
        /**
         * User's wallet address
         */
        user: string;
        /**
         * Page number for pagination (minimum: 1, default: 1)
         */
        page?: number;
        /**
         * Page size for pagination (default: 20, maximum: 50)
         */
        page_size?: number;
        /**
         * Only include pools with positions closed within this many days
         * Applied only for closed positions
         * (minimum: 1, maximum: 365, default: 120)
         */
        days_back?: number;
    };
    url: '/portfolio';
};

export type GetPortfolioResponses = {
    200: GetPortfolioResponse;
};

export type GetPortfolioResponse2 = GetPortfolioResponses[keyof GetPortfolioResponses];

export type GetPortfolioOpenData = {
    body?: never;
    path?: never;
    query: {
        /**
         * User's wallet address
         */
        user: string;
        /**
         * Page number for pagination (minimum: 1, default: 1)
         */
        page?: number;
        /**
         * Page size for pagination (default: 20, maximum: 50)
         */
        page_size?: number;
        /**
         * Sort direction, default is DESC
         */
        sort_direction?: SortDirection;
        /**
         * Sort by field, default is current_balances
         */
        sort_by?: GetOpenPortfolioSort;
    };
    url: '/portfolio/open';
};

export type GetPortfolioOpenErrors = {
    /**
     * Invalid user address or query parameters
     */
    400: unknown;
};

export type GetPortfolioOpenResponses = {
    /**
     * Successfully retrieved open portfolio
     */
    200: GetOpenPortfolioResponse;
};

export type GetPortfolioOpenResponse = GetPortfolioOpenResponses[keyof GetPortfolioOpenResponses];

export type GetPortfolioTotalData = {
    body?: never;
    path?: never;
    query: {
        /**
         * User's wallet address
         */
        user: string;
    };
    url: '/portfolio/total';
};

export type GetPortfolioTotalErrors = {
    /**
     * Invalid user address or unsupported product type
     */
    400: unknown;
};

export type GetPortfolioTotalResponses = {
    /**
     * Successfully retrieved portfolio totals
     */
    200: PortfolioTotalResponse;
};

export type GetPortfolioTotalResponse = GetPortfolioTotalResponses[keyof GetPortfolioTotalResponses];

export type GetPositionsByAddressHistoricalData = {
    body?: never;
    path: {
        address: string;
    };
    query?: {
        /**
         * Filter by event type (add, remove, claim_fee, claim_reward).
         * If not provided, returns all event types.
         */
        event_type?: null | PositionEventType;
        /**
         * Order direction for sorting events by block time.
         * - `asc`: Oldest events first
         * - `desc`: Most recent events first (default)
         */
        order_direction?: null | PositionEventOrderDirection;
    };
    url: '/positions/{address}/historical';
};

export type GetPositionsByAddressHistoricalResponses = {
    200: GetPositionHistoricalEventsResponse;
};

export type GetPositionsByAddressHistoricalResponse = GetPositionsByAddressHistoricalResponses[keyof GetPositionsByAddressHistoricalResponses];

export type GetPositionsByPoolAddressPnlData = {
    body?: never;
    path: {
        pool_address: string;
    };
    query: {
        /**
         * The user's wallet address
         */
        user: string;
        /**
         * Filter positions by status: "open", "closed", or "all" (default: "all")
         */
        status?: PositionStatus;
        /**
         * Page number for pagination (minimum: 1, default: 1)
         */
        page?: number;
        /**
         * Page size for pagination (minimum: 1, maximum: 100, default: 20)
         */
        page_size?: number;
    };
    url: '/positions/{pool_address}/pnl';
};

export type GetPositionsByPoolAddressPnlResponses = {
    200: GetPoolPositionPnLResponse;
};

export type GetPositionsByPoolAddressPnlResponse = GetPositionsByPoolAddressPnlResponses[keyof GetPositionsByPoolAddressPnlResponses];

export type GetStatsProtocolMetricsData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/stats/protocol_metrics';
};

export type GetStatsProtocolMetricsErrors = {
    400: ErrorResponse;
};

export type GetStatsProtocolMetricsError = GetStatsProtocolMetricsErrors[keyof GetStatsProtocolMetricsErrors];

export type GetStatsProtocolMetricsResponses = {
    200: ProtocolMetricsResponse;
};

export type GetStatsProtocolMetricsResponse = GetStatsProtocolMetricsResponses[keyof GetStatsProtocolMetricsResponses];

export type GetWalletsByWalletLimitOrdersClosedPoolsData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
    };
    query?: {
        /**
         * Page number, 1-indexed. Default: 1
         */
        page?: number | null;
        /**
         * Number of results per page. Default: 20, max: 1000
         */
        page_size?: number | null;
    };
    url: '/wallets/{wallet}/limit_orders/closed/pools';
};

export type GetWalletsByWalletLimitOrdersClosedPoolsErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletLimitOrdersClosedPoolsError = GetWalletsByWalletLimitOrdersClosedPoolsErrors[keyof GetWalletsByWalletLimitOrdersClosedPoolsErrors];

export type GetWalletsByWalletLimitOrdersClosedPoolsResponses = {
    200: PaginationResponseClosedLimitOrderPoolSummary;
};

export type GetWalletsByWalletLimitOrdersClosedPoolsResponse = GetWalletsByWalletLimitOrdersClosedPoolsResponses[keyof GetWalletsByWalletLimitOrdersClosedPoolsResponses];

export type GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
        /**
         * Base58-encoded LB pair (pool) address
         */
        pool_address: string;
    };
    query?: {
        /**
         * Page number, 1-indexed. Default: 1
         */
        page?: number | null;
        /**
         * Number of results per page. Default: 20, max: 1000
         */
        page_size?: number | null;
    };
    url: '/wallets/{wallet}/limit_orders/closed/pools/{pool_address}';
};

export type GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressError = GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressErrors[keyof GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressErrors];

export type GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressResponses = {
    200: ClosedLimitOrdersForPoolResponse;
};

export type GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressResponse = GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressResponses[keyof GetWalletsByWalletLimitOrdersClosedPoolsByPoolAddressResponses];

export type GetWalletsByWalletLimitOrdersOpenPoolsData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
    };
    query?: {
        /**
         * Page number, 1-indexed. Default: 1
         */
        page?: number | null;
        /**
         * Number of results per page. Default: 20, max: 1000
         */
        page_size?: number | null;
    };
    url: '/wallets/{wallet}/limit_orders/open/pools';
};

export type GetWalletsByWalletLimitOrdersOpenPoolsErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletLimitOrdersOpenPoolsError = GetWalletsByWalletLimitOrdersOpenPoolsErrors[keyof GetWalletsByWalletLimitOrdersOpenPoolsErrors];

export type GetWalletsByWalletLimitOrdersOpenPoolsResponses = {
    200: PaginationResponseOpenLimitOrderPoolSummary;
};

export type GetWalletsByWalletLimitOrdersOpenPoolsResponse = GetWalletsByWalletLimitOrdersOpenPoolsResponses[keyof GetWalletsByWalletLimitOrdersOpenPoolsResponses];

export type GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
        /**
         * Base58-encoded LB pair (pool) address
         */
        pool_address: string;
    };
    query?: {
        /**
         * Page number, 1-indexed. Default: 1
         */
        page?: number | null;
        /**
         * Number of results per page. Default: 20, max: 1000
         */
        page_size?: number | null;
    };
    url: '/wallets/{wallet}/limit_orders/open/pools/{pool_address}';
};

export type GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressError = GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressErrors[keyof GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressErrors];

export type GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressResponses = {
    200: OpenLimitOrdersForPoolResponse;
};

export type GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressResponse = GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressResponses[keyof GetWalletsByWalletLimitOrdersOpenPoolsByPoolAddressResponses];

export type GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
        /**
         * Base58-encoded LB pair (pool) address
         */
        pool_address: string;
    };
    query?: never;
    url: '/wallets/{wallet}/limit_orders/pools/{pool_address}/bonus_claimed';
};

export type GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedError = GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedErrors[keyof GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedErrors];

export type GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedResponses = {
    200: BonusClaimedResponse;
};

export type GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedResponse = GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedResponses[keyof GetWalletsByWalletLimitOrdersPoolsByPoolAddressBonusClaimedResponses];

export type GetWalletsByWalletLimitOrdersSummaryData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
    };
    query?: never;
    url: '/wallets/{wallet}/limit_orders/summary';
};

export type GetWalletsByWalletLimitOrdersSummaryErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletLimitOrdersSummaryError = GetWalletsByWalletLimitOrdersSummaryErrors[keyof GetWalletsByWalletLimitOrdersSummaryErrors];

export type GetWalletsByWalletLimitOrdersSummaryResponses = {
    200: LimitOrderSummaryResponse;
};

export type GetWalletsByWalletLimitOrdersSummaryResponse = GetWalletsByWalletLimitOrdersSummaryResponses[keyof GetWalletsByWalletLimitOrdersSummaryResponses];

export type GetWalletsByWalletPoolsByPoolAddressTotalClaimsData = {
    body?: never;
    path: {
        /**
         * Base58-encoded wallet address
         */
        wallet: string;
        /**
         * Base58-encoded pool address
         */
        pool_address: string;
    };
    query?: never;
    url: '/wallets/{wallet}/pools/{pool_address}/total_claims';
};

export type GetWalletsByWalletPoolsByPoolAddressTotalClaimsErrors = {
    400: ErrorResponse;
};

export type GetWalletsByWalletPoolsByPoolAddressTotalClaimsError = GetWalletsByWalletPoolsByPoolAddressTotalClaimsErrors[keyof GetWalletsByWalletPoolsByPoolAddressTotalClaimsErrors];

export type GetWalletsByWalletPoolsByPoolAddressTotalClaimsResponses = {
    200: GetWalletTotalClaimsResponse;
};

export type GetWalletsByWalletPoolsByPoolAddressTotalClaimsResponse = GetWalletsByWalletPoolsByPoolAddressTotalClaimsResponses[keyof GetWalletsByWalletPoolsByPoolAddressTotalClaimsResponses];
